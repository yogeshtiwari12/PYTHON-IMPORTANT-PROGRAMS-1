import string,random
pass1 = input("enter password : ")
ch = "0123456789"
while ch!=pass1:
    pas = random.choices(ch,k=len(pass1))
    pas = "".join(pas)
    print(pas)
    
    if(pass1==pas):
        break
print("pasword is : ",pas)
